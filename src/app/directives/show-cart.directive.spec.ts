import { ShowCartDirective } from './show-cart.directive';

describe('ShowCartDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCartDirective();
    expect(directive).toBeTruthy();
  });
});
